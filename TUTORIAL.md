# AIWorkbench 使用教程

本教程带你从零开始，学会使用 AIWorkbench 的全部核心功能。

## 目录

- [一、准备工作](#一准备工作)
- [二、首次启动](#二首次启动)
- [三、创建你的第一个空间](#三创建你的第一个空间)
- [四、开始对话](#四开始对话)
- [五、配置面板](#五配置面板)
- [六、知识图谱记忆](#六知识图谱记忆)
- [七、技能包](#七技能包)
- [八、数据备份与迁移](#八数据备份与迁移)
- [九、快捷键一览](#九快捷键一览)
- [十、系统托盘](#十系统托盘)
- [十一、打包 exe 与安装包](#十一打包-exe-与安装包)
- [十二、常见问题 FAQ](#十二常见问题-faq)

---

## 一、准备工作

### 方式 A：直接运行 exe（推荐新手）

项目根目录有打包好的 `AIWorkbench.exe`，双击即可运行，无需安装 Python。

### 方式 B：从源码运行

```bash
# 1. 安装 Python 3.10+
# 2. 安装依赖
pip install -r requirements.txt

# 3. 运行
python main.py
```

---

## 二、首次启动

1. 双击运行后，程序会自动在你的 `文档` 目录创建数据文件夹：

```
C:\Users\你的用户名\Documents\AIWorkbench\
├── data\      数据库
├── backups\   每日备份
├── logs\      日志（保留 30 天）
└── skills\    技能包
```

2. 弹出「配置向导」，填写：

| 项目 | 说明 |
| --- | --- |
| API Key | 你的模型密钥（可留空，稍后在配置面板填） |
| 默认模型 | 下拉选择，也可手动输入 |

3. 点击「完成」，进入主界面。

> 主窗口 **关闭按钮默认最小化到系统托盘**，不会真正退出。右键托盘图标可选择「退出」。

---

## 三、创建你的第一个空间

「空间（Space）」是一个完全独立的工作环境，拥有独立的对话、记忆和配置。

1. 点击左侧面板顶部「空间」标题下的 **新建** 按钮。
2. 输入空间名称，例如「学习笔记」。
3. 空间创建后会自动切换到它。

支持：**新建 / 重命名 / 删除 / 切换**。删除空间会连同其所有对话和记忆一起删除，且不可撤销。

---

## 四、开始对话

1. 保证左侧已选中一个空间。
2. 点击对话列表下方的 **＋ 新建对话**。
3. 在底部输入框输入消息，按 **Enter** 发送（Shift+Enter 换行）。
4. AI 会以打字机效果流式输出回复。

### 对话中的操作

| 功能 | 操作 |
| --- | --- |
| 停止生成 | 发送按钮会变红为「停止」，点击即可停止；或按 Esc |
| 重新生成 | 右键对话 / 在菜单栏选择「重新生成」 |
| 编辑已发消息 | 用户气泡上的「编辑」按钮，内容回填输入框 |
| 复制消息 | 气泡上的「复制」按钮 |
| 删除消息 | 气泡上的「删除」按钮 |
| 查看时间戳 | 每条气泡右上角显示时间 |

### Token 用量

输入框上方会显示当前对话的 Token 数。当上下文过长时，程序会**自动截断**，保留 System Prompt 与最近若干条消息。

---

## 五、配置面板

点击菜单栏「编辑 → 打开配置面板」或按 **Ctrl+,**。

可配置项：

| 配置 | 说明 |
| --- | --- |
| API Keys | 支持多个 Key，自动轮换；加密存储（Fernet） |
| Base URL | OpenAI 兼容接口地址 |
| 模型名称 | 例如 gpt-4o-mini、deepseek-chat |
| Temperature | 0~2，越大越发散 |
| Top P | 0~1 |
| System Prompt | 系统提示词 |

修改后点击「保存」**立即生效，无需重启**。点击「重置配置」可恢复默认（会清空 Key）。

### 接入其他模型（DeepSeek / Kimi / 通义 等）

只要服务提供 `/v1/chat/completions` 兼容接口，改两个地方即可：

- Base URL：例如 `https://api.deepseek.com/v1`
- 模型名称：例如 `deepseek-chat`

---

## 六、知识图谱记忆

右侧「记忆图谱」标签页以**节点 + 连线**的形式展示当前空间的所有记忆。

### 挂载记忆（对话级记忆）

1. 在任意一条消息气泡上点击 **「记忆」** 按钮。
2. 输入一段记忆内容。
3. 保存后，会在图谱中生成一个节点，并**溯源到该条消息**。

也可以点击输入框上方的 **「保存记忆」** 快捷按钮，对最后一条消息挂载记忆。

### 图谱操作

| 操作 | 方法 |
| --- | --- |
| 拖拽节点 | 按住节点拖动，位置会保存 |
| 查看详情 | 右键节点 → 查看详情（含来源对话、记忆内容） |
| 复制到其他空间 | 右键节点 → 复制节点到其他空间 → 选择目标空间 |
| 删除节点 | 右键节点 → 删除节点 |

> 节点超过 100 个时，会自动简化显示前 100 个，避免卡顿。

### 记忆拼接

通过「复制节点到其他空间」，可以把一个空间的记忆节点复制到另一个空间，并自动与目标空间的图谱合并——这就是「记忆拼接」。

---

## 七、技能包

技能包是扩展 AI 能力的 Prompt 模板，格式为 `.skill`（本质是 zip 压缩包）。

### 安装

1. 打开右侧「技能」标签页。
2. 点击 **安装技能包 (.skill)**。
3. 选择 `.skill` 文件即可。

### 启用 / 禁用

在技能列表的「启用」列点击即可切换。

### 使用

在输入框输入 `@技能名 你的问题`，例如：

```
@翻译官 你好，世界
```

程序会自动把该技能的 Prompt 模板注入系统提示。

### 自己制作一个技能包

创建一个 `.skill` zip 包，内含两个文件：

```
my-skill.skill/
├── manifest.json
└── prompt.txt
```

`manifest.json`：

```json
{
  "name": "翻译官",
  "version": "1.0.0",
  "description": "中英互译助手",
  "author": "你",
  "prompt_file": "prompt.txt"
}
```

`prompt.txt` 内容示例：

```
你是一名专业翻译。用户输入中文就翻译成英文，输入英文就翻译成中文。
只输出译文，不要额外解释。
```

打包命令（把 `manifest.json` 和 `prompt.txt` 压成 zip，改后缀为 `.skill`）：

```powershell
Compress-Archive -Path manifest.json, prompt.txt -DestinationPath 翻译官.skill
```

---

## 八、数据备份与迁移

### 自动备份

每天首次启动时，程序会自动把数据库备份到 `backups\aiworkbench_日期.db`。

### 手动导出 / 导入

菜单栏「文件」菜单：

| 功能 | 说明 |
| --- | --- |
| 导出单个空间 | 导出为 `.json` |
| 导入空间 | 从 `.json` 导入 |
| 整体备份 (ZIP) | 打包数据库 + 所有空间 |
| 整体恢复 (ZIP) | 从 ZIP 恢复 |

---

## 九、快捷键一览

| 快捷键 | 功能 |
| --- | --- |
| `Ctrl+N` | 新建空间 |
| `Ctrl+Shift+N` | 新建对话 |
| `Ctrl+S` | 保存 / 导出当前对话 |
| `Ctrl+F` | 搜索当前对话 |
| `Ctrl+Shift+F` | 全局搜索所有空间 |
| `Ctrl+,` | 打开配置面板 |
| `Enter` | 发送 |
| `Shift+Enter` | 换行 |
| `Esc` | 停止生成 / 关闭弹窗 |

---

## 十、系统托盘

- 点击窗口关闭按钮 → **最小化到系统托盘**（可在配置中关闭此行为）。
- 托盘图标右键菜单：**显示主窗口** / **退出**。
- 双击托盘图标 → 恢复窗口。

---

## 十一、打包 exe 与安装包

### 打包 exe

```bash
pip install pyinstaller
pyinstaller AIWorkbench.spec
# 产物：dist\AIWorkbench.exe（单文件）
```

### 制作安装包（Inno Setup）

1. 安装 [Inno Setup](https://jrsoftware.org/isinfo.php)。
2. 用 Inno Setup 打开 `installer\AIWorkbench.iss` 并编译。
3. 生成 `output\AIWorkbench-Setup-0.1.1.exe`。

> 默认安装目录已配置为 `D:\AIWorkbench`，可在 `.iss` 文件的 `DefaultDirName` 修改。

---

## 十二、常见问题 FAQ

**Q：提示「请先在配置面板中设置 API Key」？**
A：你还没填 Key。按 `Ctrl+,` 打开配置面板，添加 API Key 并保存。

**Q：调用失败 / 网络错误？**
A：检查 Base URL 是否正确、Key 是否有效、网络是否能访问对方服务。

**Q：数据存在哪里？会不会上传？**
A：数据全部保存在本地 `Documents\AIWorkbench\`，不会上传到你的机器之外。API Key 用 Fernet 加密后存储。

**Q：换电脑怎么迁移数据？**
A：用「文件 → 整体备份 (ZIP)」，在新电脑「整体恢复 (ZIP)」即可。

**Q：有新版怎么更新？**
A：程序启动时自动检查 GitHub Releases，右下角弹窗提示，点击可跳转下载（不强制更新）。

---

## 十三、MCP 工具扩展

AIWorkbench 既是 MCP 客户端，也可作为 MCP 服务器。

### 接入外部工具（MCP 客户端）

1. 按 `Ctrl+,` 打开配置面板。
2. 在「MCP 服务器」区域点「添加 MCP 服务器」。
3. 填写：
   - 名称：随意
   - 传输方式：`stdio`（本地命令）或 `sse`（远程 URL）
   - 命令 / 参数（stdio）：例如 `python` + 参数 `mcp_server.py`
   - URL（sse）：远程 MCP 地址
4. 保存后，对话时 AI 就会自动调用这些服务器提供的工具（文件、数据库、搜索等）。

### 对外开放自己的记忆（MCP 服务器）

在项目目录运行：

```bash
pip install -r requirements.txt
python mcp_server.py
```

它把空间与知识图谱记忆暴露为 4 个工具：`list_spaces`、`list_memory`、`search_memory`、`add_memory`。在 Claude Desktop、Cursor 等客户端中配置为 stdio 服务器即可调用。

---

> 项目开源协议：Apache License 2.0
> 源码仓库：见 README 中的 GitHub 链接。